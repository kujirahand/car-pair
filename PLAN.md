# 車の乗りあわせツール 実装計画書

`AGENTS.md` の仕様に基づく、車の乗りあわせ決定Webアプリケーションの設計とアルゴリズムのアプローチについてまとめます。

## アーキテクチャ構成

保守性の高いクリーンなPHPアプリケーションとなるよう、要望通りにロジックと表示（モダンなデザイン）を分離して構成します。

### ディレクトリ構造

```text
.
├── index.php             # メインコントローラー (ルーティング処理)
├── logic/                # ビジネスロジック処理群
│   ├── Auth.php          # ログイン・セッション管理
│   ├── CsvManager.php    # `list.csv` の読み書き処理
│   ├── History.php       # `history.json` の読み書き処理
│   └── PairingAlgorithm.php # 乗りあわせアルゴリズム
├── templates/            # 画面表示 (ビュー)
│   ├── layout.php        # 共通のHTMLレイアウト・ヘッダー
│   ├── login.php         # ログイン画面
│   ├── edit_list.php     # 名簿の編集画面
│   ├── select_members.php # 今回の参加者選択画面
│   └── pairing.php       # 乗りあわせ決定・結果表示画面
├── data/                 # データ保存領域
│   ├── .htaccess         # `Deny from all` (Webアクセスブロック)
│   ├── admin.json        # 管理者パスワード定義
│   ├── workspaces.json   # ワークスペース定義
│   └── default/          # デフォルトワークスペース
│       ├── list.csv      # 名簿マスターデータ
│       └── history.json  # 過去の乗りあわせ履歴
└── public/               # 公開用アセット
    └── style.css         # リッチなUIデザイン用の独自CSS
```

### データ形式の定義

- data/admin.json: 指定の通り、平文でのアカウント管理とします。 {"admin1": "admin1-password", "admin2": "admin2-password"}
- data/default/list.csv: カラム構成: id, name, furigana, family_id, gender, is_driver, nickname, notes, participation_count。
- data/default/history.json: 誰が誰と同乗したかの過去履歴を配列の形で保存します。

### 画面・アクションの遷移 (index.php)

フロントコントローラーである index.php によって以下の処理に振り分けます。

- ?action=login: ログインフォームを表示。送信時、admin.json で認証し、セッション開始。
- ?action=logout: セッションを破棄。
- ?action=edit_list: 名簿の追加・編集・削除処理 および 一覧表示。
- ?action=select_members: 参加回数の降順で参加者一覧を表示。チェックボックスで選択し、次へ進むとセッションに選択者を一時保存。
- ?action=pairing: PairingAlgorithm を実行し、組合せの候補を表示。

「もう一回」: アルゴリズムを再実行し、別の候補パターンを提示。
「決定」: 履歴を history.json に保存し、list.csv の参加回数をインクリメントして完了。

## 乗りあわせアルゴリズムの設計

複雑な組み合わせ条件を制約充足問題として解きます。家族は分離できないため「家族ブロック」という単位で扱います。

### 絶対条件 (ハード制約 - 必ず満たす):

- 1台の車に乗る人数は 4人。
- もし1台に2人だけの場合、その2人は（「男性」＋「女性」かつ「別々の家族」）であってはならない。
-（暗黙の前提として）1台の車には必ずドライバーが1名以上必要。家族全員が非ドライバーの場合は、ドライバーのいる別の車（家族）と一緒に乗る必要があります。

### できれば満たしたい条件 (ソフト制約):

- ドライバーの分散: 原則として「1台の車につきドライバーは1名まで」を目標にします。ただし、参加者にドライバーが多い場合など、物理的に不可能な事態では要件に従い「複数のドライバーの同乗を許可」するよう制約を柔軟に緩和します（ドライバーが多ければ、ドライバー同士を同じペアにできる）。
- 過去の履歴を回避: 複数の有効な組み合わせをランダムに作成し、history.json の履歴と比較して「過去に同乗した回数の合計」が最も少ない（異なる人と組み合わさっている）結果をベスト・候補として提示します。

### 画面デザイン方針 (Vanilla CSS)

リッチでプレミアムな印象を与えるため、一般的なデフォルトデザインは避け、モダンなUIのベストプラクティス（マージンや余白の最適化、グラスモーフィズム効果、ホバーアニメーションの追加、見やすいGoogleフォントの採用など）を style.css で実装します。

### WARNING

確認事項: 「1台に最低1人のドライバーが必要」 という前提でアルゴリズムを構築して問題ないでしょうか？（ドライバー不足で配車が不可能なパターンの場合は、エラー警告を出す設計とします。）

